# File này KHÔNG dùng cho Cloudflare Pages
# Chỉ giữ lại để tham khảo

# Nếu bạn deploy qua GitHub → Cloudflare Pages:
# - KHÔNG cần file này
# - KHÔNG cần deploy worker riêng
# - Chỉ cần file functions/api/order.js

# Nếu muốn deploy worker riêng (KHÔNG khuyến nghị):
# name = "shopvd-worker"
# compatibility_date = "2024-01-01"
# main = "worker/worker.js"
